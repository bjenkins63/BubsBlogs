<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <title>Bubs Blog</title>
</head>
<body>

        <div class="container">
            <h1 class="mb-4">Bubs Blog</h1>
            <a href="/blogs/new" class="btn btn-success">New Blog</a>


        <% blogs.forEach(blog => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-subtitle text-muted mb-2"></h4>
                <%= blog.createdAt.toLocaleDateString() %>
                </div>
                <div class="card-text mb-2"><%= article.description %></div>
                <a href="blogs/<%= blog.slug %>" class="btn btn-primary">Read More</a>
                <a href="blogs/edit/<%= blog.id %>" class="btn btn-info">Edit</a>
                <form action="/blogs/<%= blog.id %>?_method=DELETE" method="POST" class="d-inline">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
        %> }) %>
    </div>

        <!-- <a href="/register">Register</a> -->
        </div>
    
</body>
</html>